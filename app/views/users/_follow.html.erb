<%= form_for(current_user.relationships.build,remote: true) do |f| %>
      <%= hidden_field_tag :follow_id, @user.id %>
      <%= f.submit 'Follow', class: 'btn btn-primary btn-block col-12' %>
    <% end %>

<%= form_with(model: article, local: true, multipart: true ) do |form| %>

      <%= form.text_area :url, placeholder: "記事のURL", rows: "10" %>
      <%= form.collection_select(:genre_id, Genre.all, :id, :name, {}, {class:"genre-select"}) %>


      <%= form.submit "SEND" %>
    <% end %>
